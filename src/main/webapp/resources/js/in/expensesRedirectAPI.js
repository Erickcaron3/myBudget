const currentURL = window.location.pathname;
const menuEls = document.querySelectorAll(".menuElement a");

function redirect(){
    for(let el of menuEls){
        if(!el === currentURL){

        }
    }
}